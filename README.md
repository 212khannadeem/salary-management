# Employee Salary Management

Java 21 Spring Boot backend. Run with `mvn spring-boot:run`; test with `mvn test`; OpenAPI UI is at `/swagger-ui.html`.

The API supports employee CRUD and effective-dated compensation history. See `docs/API_DESIGN.md` for endpoints and `docs/ASSUMPTIONS.md` for scope decisions.
