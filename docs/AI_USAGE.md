# AI usage

Codex generated the initial implementation from the supplied specification. The output was reviewed for DTO boundaries, BigDecimal currency amounts and salary-date rules, then validated through Maven builds/tests.
